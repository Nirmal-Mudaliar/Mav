import { CONSTANTS } from './urls';

export const IMAGES_SIZES = {
  small: `${CONSTANTS.imageBaseUrl}/w185`,
  medium: `${CONSTANTS.imageBaseUrl}/w342`,
  large: `${CONSTANTS.imageBaseUrl}/original`,
};
